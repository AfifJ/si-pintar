class UserModel {
  String user_id;
  String username;
  String npm;
  String password;
  String email;
  String full_name;
  String role;

  UserModel(this.user_id, this.username, this.npm, this.password, this.email,
      this.full_name, this.role);
}
